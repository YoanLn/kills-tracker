services:
  app:
    build: .
    ports:
      - "5002:8080"
    volumes:
      - ./data.db:/data/data.db
    environment:
      PORT: "8080"
      DATA_DIR: /data
    env_file:
      - .env
    restart: unless-stopped
